{
  "name": "lambda-apigw-sockets",
  "private": true,
  "scripts": {
    "print": "osls print",
    "package": "osls package",
    "deploy": "osls deploy",
    "remove": "osls remove",
    "build:dependencies": "rm -rf .requirements && python3.12 -m pip install --target .requirements -r requirements.txt",
    "test": "pytest",
    "lint": "ruff check .",
    "typecheck": "mypy app"
  },
  "devDependencies": {
    "serverless": "3.40.0",
    "wscat": "6.1.0"
  }
}
